#include <iostream>
#include <cmath>
using namespace std;
int main() {
  float a = -2.34,b=0,x=0,z=0;
  b = a - 5;
  x = abs(b) - sin(a);
  x = a / 3;
  a = sqrt(pow(a, 2) + 2014) * cos(2*a);
  z= x + a-3;
  cout << z;
  return 0;
}